{
  "name": "move-challenge-firestore-tests",
  "private": true,
  "description": "Testes das regras de segurança do Firestore do Move Challenge",
  "scripts": {
    "test": "mocha --timeout 15000 \"test/**/*.test.js\"",
    "test:emulador": "firebase emulators:exec --only firestore --project demo-move-challenge \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^14.11.0",
    "mocha": "^10.8.2"
  }
}
